### Initializing with Another BikramSambat Instance

You can initialize a `BikramSambat` instance using another `BikramSambat` instance.

```jsx copy
const originalDate = new BikramSambat('2078-05-15')
const newDate = new BikramSambat(originalDate)
```
