### `isAfter(date: BikramSambat): boolean`

Returns `true` if the provided Bikram Sambat date is after the current date.

```jsx copy
const date1 = new BikramSambat('2078-05-15')
const date2 = new BikramSambat('2078-06-15')
const isAfter = date1.isAfter(date2) // true
```
