### `getNextDay(): BikramSambat`

Returns the next day of the current Bikram Sambat date.

```jsx copy
const date = new BikramSambat('2078-05-15')
const nextDay = date.getNextDay() // Date of the next day
```
