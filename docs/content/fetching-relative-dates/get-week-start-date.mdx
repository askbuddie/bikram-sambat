### `getWeekStartDate(): BikramSambat`

Returns the start date of the current week (Sunday) for the Bikram Sambat date.

```jsx copy
const date = new BikramSambat('2078-05-15')
const weekStart = date.getWeekStartDate() // Start date of the current week
```
