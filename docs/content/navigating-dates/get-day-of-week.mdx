### `getDayOfWeek(): number`

Returns the day of the week (0 for Sunday, 1 for Monday, etc.) of the Bikram Sambat date.

```jsx copy
const date = new BikramSambat('2078-05-15')
const dayOfWeek = date.getDayOfWeek() // 3 (Example value, Wednesday)
```
