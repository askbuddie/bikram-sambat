### `isLeapYear(): boolean`

Returns `true` if the year of the Bikram Sambat date is a leap year.

```jsx copy
const date = new BikramSambat('2078-05-15')
const isLeap = date.isLeapYear() // false (Example value)
```
