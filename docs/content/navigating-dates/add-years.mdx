### `addYears(years: number): BikramSambat`

Adds the given number of years to the Bikram Sambat instance.

```jsx copy
const date = new BikramSambat('2078-05-15')
date.addYears(3) // Adds 3 years to the date
```
