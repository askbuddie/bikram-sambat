### `startOfMonth(): BikramSambat`

Returns the first day of the current month for the Bikram Sambat date.

```jsx copy
const date = new BikramSambat('2078-05-15')
const startOfMonth = date.startOfMonth() // First day of the current month
```
