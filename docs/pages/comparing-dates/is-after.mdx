### `isAfter(date: BikramSambat): boolean`

Returns `true` if the current date is after the provided Bikram Sambat date.

```jsx
const date1 = new BikramSambat("2078-05-15");
const date2 = new BikramSambat("2078-04-15");
const isAfter = date1.isAfter(date2); // true
```
