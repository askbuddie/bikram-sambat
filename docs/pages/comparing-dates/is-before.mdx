### `isBefore(date: BikramSambat): boolean`

Returns `true` if the provided Bikram Sambat date is before tha current date.

```jsx copy
const date1 = new BikramSambat('2078-05-15')
const date2 = new BikramSambat('2078-04-15')
const isBefore = date1.isBefore(date2) // true
```
