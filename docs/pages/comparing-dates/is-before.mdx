### `isBefore(date: BikramSambat): boolean`

Returns `true` if the current date is before the provided Bikram Sambat date.

```jsx
const date1 = new BikramSambat("2078-05-15");
const date2 = new BikramSambat("2078-06-15");
const isBefore = date1.isBefore(date2); // true
```
