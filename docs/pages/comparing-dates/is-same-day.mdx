### `isSameDay(date: BikramSambat): boolean`

Returns `true` if the provided Bikram Sambat date is the same as the current date.

```jsx copy
const date1 = new BikramSambat('2078-05-15')
const date2 = new BikramSambat('2078-05-15')
const isSameDay = date1.isSameDay(date2) // true
```
