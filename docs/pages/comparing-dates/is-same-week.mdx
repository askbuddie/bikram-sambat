### `isSameWeek(date: BikramSambat): boolean`

Returns `true` if the provided Bikram Sambat date is in the same week as the current date.

```jsx
const date1 = new BikramSambat("2078-05-15");
const date2 = new BikramSambat("2078-05-17");
const isSameWeek = date1.isSameWeek(date2); // true
```
