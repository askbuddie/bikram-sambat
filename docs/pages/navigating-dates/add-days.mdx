### `addDays(days: number): BikramSambat`

Adds the given number of days to the Bikram Sambat instance.

```jsx
const date = new BikramSambat("2078-05-15");
date.addDays(10); // Adds 10 days to the date
```
