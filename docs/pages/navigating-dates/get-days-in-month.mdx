### `getDaysInMonth(): number`

Returns the number of days in the month of the Bikram Sambat date.

```jsx
const date = new BikramSambat("2078-05-15");
const daysInMonth = date.getDaysInMonth(); // 32 (Example value)
```
