### `getPreviousMonth(): Month | null`

Returns the previous month of the Bikram Sambat date as an enum value.

```jsx copy
const date = new BikramSambat('2078-05-15')
const prevMonth = date.getPreviousMonth() // An enum representing the previous month
```
