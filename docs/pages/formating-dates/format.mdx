### `format(dateFormat: DateFormat): string`

Formats the Bikram Sambat date into the specified format.

```jsx copy
const date = new BikramSambat('2078-05-15')
const formattedDate = date.format('YYYY-MM-DD') // "2078-05-15"
```
