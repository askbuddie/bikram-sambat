### `getWeekdayNames(language?: LanguageCode): string[]`

Returns an array of weekday names in the specified language (default is Nepali).

```jsx
const weekdayNames = BikramSambat.getWeekdayNames(); // Array of weekday names
```
